---
default: patch
---

# Add fallback in MCP apps to get toolName from `_meta`

Provides a fallback in MCP apps to get the executed tool name from `_meta.toolName` when the host does not provide `toolInfo`.
